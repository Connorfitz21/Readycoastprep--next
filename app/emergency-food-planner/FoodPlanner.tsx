"use client";
import { useMemo, useState } from "react";
export default function FoodPlanner(){const [adults,setAdults]=useState(2),[children,setChildren]=useState(0),[days,setDays]=useState(7),[buffer,setBuffer]=useState(10);const meals=useMemo(()=>((adults+children)*days*3),[adults,children,days]);const buffered=Math.ceil(meals*(1+buffer/100));return <div className="rounded-2xl border border-slate-800 bg-slate-900 p-6 sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Adults<input className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 p-3" type="number" min="0" value={adults} onChange={e=>setAdults(Number(e.target.value))}/></label><label className="text-sm font-semibold">Children<input className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 p-3" type="number" min="0" value={children} onChange={e=>setChildren(Number(e.target.value))}/></label><label className="text-sm font-semibold">Days<input className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 p-3" type="number" min="1" max="30" value={days} onChange={e=>setDays(Number(e.target.value))}/></label><label className="text-sm font-semibold">Meal buffer<select className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 p-3" value={buffer} onChange={e=>setBuffer(Number(e.target.value))}><option value="0">0%</option><option value="10">10%</option><option value="20">20%</option><option value="30">30%</option></select></label></div><div className="mt-7 border-t border-slate-700 pt-6"><p className="text-sm text-slate-400">Meal slots to cover</p><p className="mt-1 text-4xl font-black text-amber-300">{buffered}</p><p className="mt-4 text-sm leading-6 text-slate-400">This deliberately plans meal coverage rather than prescribing calories. Choose shelf-stable foods your household already eats and account for allergies, infant feeding, medical diets, no-power cooking constraints and medications. Individual nutrition needs vary.</p></div></div>}
